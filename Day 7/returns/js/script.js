/*
 * Bryan Gillespie
 * 3-9-2015
 * Section 00
 * Day 7 Returns
 */

function calcArea(x,y) {
    var area = x*y;
    return area;
}

console.log("Area: " + calcArea(10,20));

function circleArea(r) {
    return r * r * Math.PI;
}